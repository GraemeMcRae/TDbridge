# TDbridge requirements
# Install with: pip install -r requirements.txt

# Discord bot
discord.py>=2.3.2

# Telegram bot (async, supports webhook mode)
python-telegram-bot[webhooks]>=21.0

# Google Sheets
gspread>=6.0.0
oauth2client>=4.1.3

# Environment / config
python-dotenv>=1.0.0

# HTTP server for Telegram webhooks (aiohttp used internally by python-telegram-bot)
aiohttp>=3.9.0

# Timezone support (stdlib zoneinfo used; tzdata needed on Windows)
tzdata>=2024.1
